import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        marginTop: "50px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}>
      <h1>Not found this page</h1>
    </div>
  );
};

export default NotFound;
